/**
 * @param {number} numRows
 * @return {number[][]}
 */
var generate = function (numRows) {
    var triangle = []
    if (numRows == 1) {
        triangle.push([1])
        return triangle
    } else {
        // for now incomplete
    }
};


console.log(generate(1))